const config = {
    // 动画配置
    animation: {
        duration: 1000,
        easing: 'cubic-bezier(0.4, 0, 0.2, 1)',
        delay: 200
    },
    
    // API配置（如果需要）
    api: {
        baseUrl: 'https://api.example.com',
        timeout: 5000
    },
    
    // 主题配置
    theme: {
        primary: '#0ef',
        secondary: '#b7b2a9',
        background: '#1f242d'
    }
};

export default config; 